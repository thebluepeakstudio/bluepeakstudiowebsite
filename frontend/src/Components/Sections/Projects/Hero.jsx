import PageHero from "../../Layout/PageHero";

const Hero = () => {
  return (
    <PageHero
      title="Projects"
      description="A selection of websites and web applications we've built — from brand sites and e-commerce stores to booking platforms and business dashboards."
    />
  );
};

export default Hero;
