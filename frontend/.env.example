VITE_BACKEND_URL=http://localhost:10000
# Public site URL — used for canonical links, Open Graph, and structured data
VITE_SITE_URL=https://bluepeakstudio.in
# Development: CRM at http://localhost:5173/admin-panel
# Production:  CRM at https://crm.bluepeakstudio.in
# Use the backend origin only — do not include /api (routes add that automatically).
